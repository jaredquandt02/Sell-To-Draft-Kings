# Sell-To-Draft-Kings
New fantasy gambling mode — Gladiator

## Run locally

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

Core sim logic lives in `src/simulator.ts`.
